org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=true

# NOTE: If you see errors about "build uses a Java 8 JVM" or "Dependency requires at least JVM runtime version 11",
# you must run Gradle with a Java 11+ runtime. On CI or local machines set one of the following:
# 1) Set JAVA_HOME to a JDK 11+ installation (recommended):
#    PowerShell example:
#      $env:JAVA_HOME = 'C:\\Program Files\\Java\\jdk-17'
#      [Environment]::SetEnvironmentVariable('JAVA_HOME', 'C:\\Program Files\\Java\\jdk-17', 'User')
# 2) Or set Gradle to use a specific JDK by adding a line below with your JDK path:
#    org.gradle.java.home=C:\\Program Files\\Java\\jdk-17
# After installing JDK 11+ and setting one of the above, re-run the build (flutter build apk --debug).
